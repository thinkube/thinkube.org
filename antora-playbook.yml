# Antora playbook for the Thinkube documentation site.
# Base path per deploy target is set via the `--url` CLI flag, NOT here:
#   cluster (default):  antora antora-playbook.yml            -> base "/"
#   GitHub Pages (SL-6): antora --url /thinkube.org/ ...      -> base "/thinkube.org/"
site:
  title: Thinkube
  start_page: thinkube-docs::index.adoc
  url: /

content:
  sources:
    - url: .
      branches: HEAD
      start_path: .
    # Multi-repo / docs-with-code: a template's docs live in its OWN repo and are
    # aggregated here at build time. Proven locally (SL-3). Disabled for the
    # deployed build until the source repo is public or build credentials are
    # configured (tkt-texplitter is currently private):
    # - url: https://github.com/kubexlat/tkt-texplitter.git
    #   branches: main
    #   start_path: docs

ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true
  supplemental_files: ./supplemental-ui

asciidoc:
  extensions:
    - asciidoctor-kroki
  attributes:
    # d2 diagrams are rendered via a Kroki server and fetched at BUILD time so
    # the published site embeds the image and needs no runtime Kroki access.
    kroki-server-url: https://kroki.io
    kroki-fetch-diagram: true
    kroki-default-format: svg

output:
  dir: ./build/site

antora:
  extensions:
    - require: '@antora/lunr-extension'
      index_latest_only: true

runtime:
  fetch: true
  log:
    failure_level: warn
