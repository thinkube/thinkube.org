= Serve a model you fine-tuned
:page-type: playbook
:page-area: models
:page-tagline: Give a registered fine-tune a catalogue entry, load it on a GPU and call it through the gateway like any other model
:page-difficulty: intermediate
:page-time: 5 min
:page-risk: low
:page-tags: Fine-tuning, LLM Gateway, vLLM, Model catalogue
:page-order: 80
:page-updated: 2026-09-16

== Overview

=== Basic idea

A fine-tune becomes a model you can call in two steps. Once it is served, nothing about calling it is special.

* *Registered.* The merged weights are a version in Thinkube Experiments, linked to the run that trained them, and thinkube-control has a completed registration for the model. Registering puts the weights in your storage.
* *In the catalogue.* A catalogue entry tells the gateway how to serve the weights: the backend, the reasoning format, whether it calls tools. With both, the model is `deployable`.

In this release the zebra-grpo notebook writes the registration record and the catalogue entry itself, in its section 14b. The next releases record a registered fine-tune for you.
* *Served.* A load puts it on a GPU, and it answers at `llm.<your domain>` by its name, through the same API as every other model.

=== What you'll accomplish

You check that `zebra-rules-qwen35-4b`, the model from xref:playbooks/fine-tune-on-rewards-a-program-checks.adoc[Fine-tune a model on rewards a program checks], has its catalogue entry, load it on vLLM, have it translate a puzzle's clues through the gateway for the solver to finish, and unload it.

=== What to know before starting

*Required*

* xref:playbooks/fine-tune-on-rewards-a-program-checks.adoc[Fine-tune a model on rewards a program checks]: the run that registers the model.
* xref:playbooks/call-your-models-from-an-app-or-a-script.adoc[Call your models from an app or a script]: the gateway and its token.

*Optional*

* xref:control:llm-serving.adoc[LLM serving and models]: model states, sizing and routing.

=== Supported hardware

[cols="1,1,1,2",options="header"]
|===
| Node | Architecture | GPU | On the reference run

| tkamd1 | amd64 | none | —
| tkamd2 | amd64 | 2 × RTX 3090, 24 GB each | `zebra-rules-qwen35-4b`, served by vLLM
| tkspark | arm64 | GB10, 121.7 GB unified | the notebook that called it
|===

The model is 8.7 GB of weights in BF16, the size of its base, `unsloth/Qwen3.5-4B`. It fits one RTX 3090 or the DGX Spark.

=== Prerequisites

*Platform*

* Thinkube running, with Thinkube IDE open. Ask Claude: _"what's running?"_
* The fine-tune registered, with the notebook's section 14b run. Ask Claude: _"is zebra-rules-qwen35-4b deployable?"_

*Components*

* The optional component vLLM. Ask Claude: _"is vLLM installed?"_ If it is not, ask _"install vLLM"_.

=== Time & risk

* *Estimated time:* 5 min. On the recorded run the load and the call through the gateway took about 5 minutes together.
* *Risk level:* Low. A load that does not fit a node is refused before anything starts.
* *Rollback:* Unload the model. The registered version stays.
* *Last updated:* 2026-09-16. Written from the run recorded in the zebra-grpo notebook, commit ef3a82b of `thinkube-notebooks-examples`; the catalogue entry checked on the reference cluster.

== Instructions

=== Step 1. Check the catalogue entry

The platform reads the catalogue from two `models.json` files: the platform's, in `thinkube/thinkube-metadata`, and yours, in the private repository `<your GitHub user>/<your GitHub user>-metadata`. An entry in yours with the same `id` wins. The notebook's section 14b writes the entry for `zebra-rules-qwen35-4b` in yours.

Ask Claude:

----
› show the catalogue entry for zebra-rules-qwen35-4b
----

Claude calls `get_model_catalog`.

Expected output, from the reference cluster:

----
id: zebra-rules-qwen35-4b
name: Zebra Rules (Qwen3.5-4B fine-tune)
server_type: [vllm]
quantization: BF16
reasoning_format: qwen3
tool_use: false
is_finetuned: true
----

=== Step 2. Make a fine-tune of your own loadable

A model you train under another name needs the same two records. Copy section 14b of the zebra-grpo notebook into your notebook, after your registration, and change `CATALOG_ENTRY` to your model. The cell writes the entry to `models.json` in your `-metadata` repository, records the registration, and waits until the gateway lists the model; the platform reads the catalogue again within 5 minutes.

The zebra model's entry is a good start:

[source,json]
----
{
  "id": "zebra-rules-qwen35-4b",
  "name": "Zebra Rules (Qwen3.5-4B fine-tune)",
  "params_b": 4.7,
  "active_params_b": null,
  "quantization": "BF16",
  "context_length": 262144,
  "description": "Qwen3.5-4B GRPO-tuned to translate zebra-puzzle clues into formal rules. Produced by the zebra-grpo example notebook.",
  "server_type": ["vllm"],
  "task": "text-generation",
  "reasoning_format": "qwen3",
  "tool_use": false,
  "stop_tokens": [],
  "license": "apache-2.0",
  "gated": false,
  "serving_name": "zebra-rules-qwen35-4b",
  "is_finetuned": true
}
----

The `id` is the name the model was registered under in Thinkube Experiments. Take `params_b`, `context_length` and `reasoning_format` from the base model's entry.

=== Step 3. Load it

Ask Claude:

----
› load zebra-rules-qwen35-4b on tkamd2
----

Claude calls `load_llm_model` and follows `get_llm_model_status` until the state is `available`.

Or by hand: in thinkube-control open *LLM Gateway*, choose *Load* on the model, pick the node and choose *Load Model*.

Expected output, from the recorded run:

----
state: loading
backend_id: vllm-tkamd2
----

and then:

----
state: available
backend_id: vllm-tkamd2
----

=== Step 4. Call it and let the solver finish

The model writes the rules; the solver in your code deduces the grid. This is the shape the fine-tune was trained for.

In the zebra-grpo notebook, after its puzzles and solver are defined:

[source,python]
----
from tk_llm import get_openai_client

client = get_openai_client()
pz = eval_puzzles[1]
reply = client.chat.completions.create(
    model="zebra-rules-qwen35-4b",
    messages=[{"role": "user", "content": translation_prompt(pz)}],
    extra_body={"chat_template_kwargs": {"enable_thinking": False}},
)
rules = parse_rules(reply.choices[0].message.content) or []
grid, n_valid, n_invalid = solve_rules(rules, pz) if rules else (None, 0, 0)
print(f"Gateway translation: {len(rules)} rules, valid={n_valid}, acc={grade_grid(grid, pz):.2f}")
----

Expected output, from the recorded run:

----
Gateway translation: 15 rules, valid=15, acc=1.00
----

Fifteen clues, fifteen valid rules, and every cell of the grid right, on a puzzle the model never trained on.

=== Step 5. Unload it

Ask Claude:

----
› unload zebra-rules-qwen35-4b
----

Or by hand: in *LLM Gateway*, choose *Unload* on the model.

Expected output:

----
state: deployable
message: Model unloaded successfully
----

The registered version stays, and the next load reads it from your storage.

=== Step 6. Next steps

* xref:playbooks/call-your-models-from-an-app-or-a-script.adoc[Call your models from an app or a script]: call the fine-tune from an app.
* xref:playbooks/load-a-model-on-the-node-and-context-you-choose.adoc[Load a model on the node and context you choose]: choose the node and the context.
* xref:playbooks/fine-tune-on-rewards-a-program-checks.adoc[Fine-tune a model on rewards a program checks]: train the next version.

== Troubleshooting

[cols="1,1,2",options="header"]
|===
| Symptom | Cause | Fix

| The load answers `Model 'zebra-rules-qwen35-4b' not found`
| The model is registered in Thinkube Experiments, and its catalogue entry or its registration record is missing
| Run section 14b of the notebook, as in Step 2.
|===
